#!/usr/bin/env bash
set -euo pipefail

action="${1:-boot}"

if [[ "$action" != "switch" && "$action" != "boot" ]]; then
	echo "Usage: nix_full_update [switch|boot]" >&2
	exit 1
fi

nix_update "$action"
nix_clean "$action"
