{
  # Replace null with an input-remapper output symbol or macro. Unconfigured
  # buttons keep the shortcuts emitted by the tablet firmware.
  pad1Top = "KEY_LEFTCTRL + KEY_LEFTSHIFT + KEY_P"; # Xournal++ pen
  pad2 = "KEY_LEFTCTRL + KEY_LEFTSHIFT + KEY_E"; # I
  pad3 = "KEY_LEFTCTRL + KEY_LEFTSHIFT + KEY_G"; # [
  pad4 = "KEY_F13"; # Xournal++ laser pointer
  pad5 = null; # Space
  pad6 = null; # Ctrl+S; example: "KEY_LEFTCTRL + KEY_S"
  pad7 = null; # Ctrl+KP+
  pad8Bottom = "KEY_LEFTCTRL + KEY_Z"; # Undo

  # The native driver does not emit usable events for either pen side button.
}
