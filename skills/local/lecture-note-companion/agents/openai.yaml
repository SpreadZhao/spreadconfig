interface:
  display_name: "Lecture Note Companion"
  short_description: "Build illustrated lecture notes with a separate full transcript"
  default_prompt: "Use $lecture-note-companion to follow this lecture step by step, distinguish mainline chunks from side questions, and finish with a structured illustrated note plus a separate cleaned full-transcript resource."
policy:
  allow_implicit_invocation: true
